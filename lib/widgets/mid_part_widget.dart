
import 'package:flutter/material.dart';

class MidPartWidget extends StatelessWidget {
  const MidPartWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.symmetric(horizontal: 20),
      color: Color(0xffffffff),
      child: Column(
        children: [
        
      ]),
    );
  }
}